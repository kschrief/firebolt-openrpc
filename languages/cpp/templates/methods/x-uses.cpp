    /* ${method.name} - ${method.description} */
    void ${info.Title}Impl::request${method.Name}(const ${method.Name}Usage& type, const std::optional<std::string>& message, I${info.Title}AsyncResponse& response, Firebolt::Error *err)
    {
    }
    void ${info.Title}Impl::abort${method.Name}(I${info.Title}AsyncResponse& response, Firebolt::Error *err)
    {
    }
